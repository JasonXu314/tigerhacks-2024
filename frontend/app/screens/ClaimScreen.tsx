import { View, SafeAreaView, Text, Button } from "react-native";

const ClaimScreen = () => {
    return (
        <SafeAreaView>
            
        </SafeAreaView>
    );
}
 
export default ClaimScreen;