import { View, SafeAreaView, Text, Button } from "react-native";

const MapScreen = () => {
    return (
        <SafeAreaView>
            
        </SafeAreaView>
    );
}
 
export default MapScreen;