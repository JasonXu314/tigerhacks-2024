import { View, SafeAreaView, Text, Button } from "react-native";

const HomeScreen = () => {
    return (
        <SafeAreaView>
            
        </SafeAreaView>
    );
}
 
export default HomeScreen;