import { View, SafeAreaView, Text, Button } from "react-native";

const RecipesScreen = () => {
    return (
        <SafeAreaView>
            
        </SafeAreaView>
    );
}
 
export default RecipesScreen;